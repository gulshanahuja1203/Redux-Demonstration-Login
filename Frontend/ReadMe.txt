To run this project, download and extract all the files.
Open the folder in Visual Studio Code and run this command in the terminal "npm install" to get the node_modules
Go through the files to better understand the Redux concept.
Run "npm start" in the terminal to run the project.
